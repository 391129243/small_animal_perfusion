<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content">
    <ImageView
      	android:id="@+id/battery_img"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:src="@drawable/battery_10"
	    android:contentDescription="@null"/>
    <!-- 电量 -->
	<TextView 
	    android:id="@+id/battery_level_txt"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:layout_toRightOf="@id/battery_img"
	    android:layout_alignBottom="@id/battery_img"
	    android:textColor="@color/white"
	    android:textSize="16sp"
	    android:text="12%"/>
	<!-- 充电状态 -->
	<ImageView 
	    android:id="@+id/battery_charging_img"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:layout_above="@id/battery_level_txt"
	    android:layout_toRightOf="@id/battery_img"
	    android:layout_alignTop="@id/battery_img"
	    android:src="@drawable/battery_charging"
	    android:contentDescription="@null"/>
</RelativeLayout>
